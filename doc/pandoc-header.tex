% Nagłówek używany przy składaniu README.md do PDF (pandoc + xelatex).
\usepackage{graphicx}
\setkeys{Gin}{keepaspectratio}

% Łamanie długich linii w blokach kodu.
\usepackage{fvextra}
\fvset{breaklines=true,breakanywhere=true,fontsize=\small}

% Obrazki (zrzuty i diagramy) dokładnie w miejscu wstawienia, bez "pływania".
\usepackage{float}
\floatplacement{figure}{H}
